# {{ title }}

| Policy | Device Type | Intune UX Support |
|--------|-----------|-------|
{% for policy_file in results -%}
{% set result = results[policy_file] -%}
| [ {{ result.title }} ]( {{ result.file | urlencode }}) | {{ result.result.entry_type }} | {{result.result.intune_ux_support.isValid() }}
{% endfor %}