# {{ title }}

| Policy | Device Type |
|--------|-----------|
{% for policy_file in results -%}
{% set result = results[policy_file] -%}
| [ {{ result.title }} ]( {{ result.file | urlencode }}) | {{ result.result.entry_type }} |
{% endfor %}